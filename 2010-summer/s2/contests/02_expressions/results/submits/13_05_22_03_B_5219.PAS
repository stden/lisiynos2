program regex;
Var a:array [1..100] of char;
b:array [1..100] of char;
g,s:string;
n,i,m,g,h:longint;
begin
assign (input, 'regex.in');
assign (output, regex.out');
reset (input);
rewrite (output);
ReadLn(e, d);
m:length(s)
readln(s);
for i:=1 to m do
    a[i]:=s[i];
readln(g);
n:=length(g);
i:=1;
for i:=1 to n   do
    b[i]:=s[i];
WriteLn('YES');
close(input);
close(output);
end.