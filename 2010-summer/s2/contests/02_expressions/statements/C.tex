\begin{problem}{–азделение выражени€ на лексемы}{lexem.in}{lexem.out}{1 секунда}{64 мегабайта}

«адано числовое выражение, заканчивающеес€ точкой. Ќеобходимо разбить его на лексемы и вывести каждую на новой строке.
√арантируетс€, что исходное выражение корректно.

¬ выражении могут встречатьс€ знаки сложени€, вычитани€, умножени€ и скобки. ѕриоритет операций стандартный. ¬се числа в выражении целые и 
принадлежат диапазону LongInt.

\InputFile

ѕерва€ строка входного файла содержит заданное выражение. ≈го длина не превосходит 100 знаков. √арантируетс€, что выражение заканчиваетс€ точкой.

\OutputFile

¬ыведите все встречающиес€ лексемы выражени€ по пор€дку и ровно по одной на каждой строке.

\Example

\begin{example}
\exmp{
1+(2*2-3).
}{
1
+
(
2
*
2
-
3
)
}%
\end{example}

\end{problem}
