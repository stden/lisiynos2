\begin{problem}{«начение логического выражени€}{logexpr.in}{logexpr.out}{1 секунда}{64 мегабайта}

«адано логическое выражение, заканчивающеес€ точкой. Ќеобходимо посчитать его значение или сказать, что оно содержит ошибку. 
¬ выражении могут встречатьс€ знаки \texttt{$!$} (отрицание), \texttt{$\&$} (логическое ``и''), \texttt{$|$} (логическое ``или''), \texttt{\fsd} (XOR) и скобки.
—амый большой приоритет у отрицани€, чуть меньше у \texttt{$\&$}, у оставшихс€ операций приоринет одинаково маленький.
¬се числа в выражении либо 0, либо 1.

\InputFile

ѕерва€ строка входного файла содержит заданное выражение. ≈го длина не превосходит 100 знаков. √арантируетс€, что выражение заканчиваетс€ точкой.

\OutputFile

¬ыведите в выходной файл значение этого выражени€ (0 или 1).

\Example

\begin{example}
\exmp{
1|(0\&0\fsd1).
}{
1
}%
\end{example}

\end{problem}