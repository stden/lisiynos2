\begin{problem}{«начение выражени€}{expr.in}{expr.out}{1 секунда}{64 мегабайта}

«адано числовое выражение, заканчивающеес€ точкой. Ќеобходимо посчитать его
значение или сказать, что оно содержит ошибку. ¬ выражении могут
встречатьс€ знаки сложени€, вычитани€, умножени€ и скобки. ѕриоритет
операций стандартный. ¬се числа в выражении целые и принадлежат диапазону
LongInt. “акже гарантируетс€ что все промежуточные вычислени€ умещаютс€ в
этот тип.

\InputFile

ѕерва€ строка входного файла содержит заданное выражение. ≈го длина не
превосходит 100 знаков. √арантируетс€, что выражение заканчиваетс€ точкой.

\OutputFile

¬ыведите в выходной файл значение этого выражени€ или слово `\texttt{WRONG}', если
значение не определено.

\Example

\begin{example}
\exmp{
1+(2*2-3).
}{
2
}%
\exmp{
1+a+1.
}{
WRONG
}%
\end{example}

\end{problem}