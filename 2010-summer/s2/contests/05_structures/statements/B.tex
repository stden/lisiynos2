\begin{problem}{’ипуй!}{heap.in}{heap.out}{2 секунда}{64 мегабайт}

¬ этой задаче вам необходимо организовать структуру данных Heap дл€ хранени€ целых чисел, 
над которой определены следующие операции:

a)	\texttt{Insert(N)}~--- добавить в Heap число $N$

b)	\texttt{Extract}~--- достать из Heap наибольшее число (удалив его при этом)

\InputFile

¬о входном файле записано количество команд $N$ ($1 \le N \le 100000$), потом последовательность из $N$ команд, кажда€ в своей строке. 
 ажда€ команда имеет такой формат: ``0 <число>'' или ``1'', обозначающие 
соответственно операции \texttt{Insert(<число>)} и \texttt{Extract}. 
√арантируетс€, что при выполенении команды \texttt{Extract} в структуре находитс€ по крайней мере один элемент.

\OutputFile

¬ выходной файл необходимо дл€ каждой команды извлечени€ необходимо отдельной вывести число, полученное 
при выполнении команды \texttt{Extract}.

\Example

\begin{example}
\exmp{
7
0 100
0 10
1
0 5
0 30
0 50
1
}{
100
50
}%
\end{example}

\end{problem}
