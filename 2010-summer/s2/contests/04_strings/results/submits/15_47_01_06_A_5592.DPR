program A22;
var
  a:array[1..1000000] of integer;
  S, T, m:string;
  k, c, i:integer;
begin
     {$Q-}  ;
     Assign(input,'Substr.in');
     Reset(input);
     Assign(output, 'Substr.out') ;
     rewrite(output);
     readln(S,T);

     writeln('3')

end.

