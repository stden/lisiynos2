\def\name{wordgame}%
\begin{problem}{»гра в слова}{\name.in}{\name.out}{}{}

ƒаны два слова $A$ и $B$. ¬ывести \t{YES} или \t{NO} в зависимости от того, можно ли получить $A$ путем вычеркивани€ одной или более букв из $B$.

\InputFile

¬о входном файле две строки (не более $200$ символов кажда€, содержат только строчные английские буквы), в первой строке записано $A$, во второй строке - $B$.

\OutputFile

¬ выходной файл вывести одно из двух слов \t{YES}, в случае если $A$ можно получить из $B$, и \t{NO} в противном случае.

\Examples

\begin{example}
\exmp{ \input \name/01 }{ \input \name/01.a }% 
\exmp{ \input \name/02 }{ \input \name/02.a }% 
\exmp{ \input \name/03 }{ \input \name/03.a }% 
\end{example}

\end{problem}
