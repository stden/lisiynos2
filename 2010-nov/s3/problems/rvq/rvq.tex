\begin{problem}{Range Variation Query}{rvq.in}{rvq.out}{2 секунды}{}{}

¬ начальный момент времени последовательность $a_n$ задана следующей формулой:
$a_n = n^2 \bmod 12345 + n^3 \bmod 23456$.

“ребуетс€ много раз отвечать на запросы следующего вида:
\begin{itemize}
\item найти разность между максимальным и минимальным значением
среди элементов $a_i, a_{i+1}, ..., a_j$;
\item присвоить элементу $a_i$ значение $j$.
\end{itemize}

\InputFile

ѕерва€ строка входного файла содержит натуральное число $k$ --- количество
запросов ($k \le 100\,000$).
—ледующие $k$ строк содержат запросы, по одному на строке. «апрос номер $i$ описываетс€
двум€ целыми числами $x_i$, $y_i$.

≈сли $x_i > 0$, то требуетс€ найти разность между максимальным и минимальным
значением среди элементов $a_{x_i} ... a_{y_i}$. ѕри этом $1 \le x_i \le y_i \le 100\,000$.

≈сли $x_i < 0$, то требуетс€ присвоить элементу $a_{|x_i|}$ значение $y_i$.
ѕри этом $-100\,000 \le x_i \le -1$ и $|y_i| \le 100\,000$.

\OutputFile

ƒл€ каждого запроса первого типа в выходной файл требуетс€ вывести одну строку,
содержащую разность между максимальным и минимальным значением на соответствующем
отрезке.

\Example

\begin{example}
\exmp{7
1 3
2 4
-2 -100
1 5
8 9
-3 -101
2 3
}{
34
68
250
234
1
}%
\end{example}

\end{problem}
