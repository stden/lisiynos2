\begin{problem}{ оличество циклов}
{numcycle.in}{numcycle.out}{2 секунды}{64 мегабайта}

 ‘ормально, {\it путь} в графе --- это чередующа€с€ последовательность
 вершин и р\"ебер $u_1, \, e_1, \, u_2, \, e_2, \, u_3, \ldots, \, u_k$,
 начинающа€с€ и заканчивающа€с€ вершиной и така€, что любые соседние вершина
 и ребро в ней инцидентны.

 {\it ÷икл} --- это путь, начальна€ и конечна€ вершины которого совпадают.
 ¬ цикле должно быть хот€ бы одно ребро.

 {\it ѕростой путь} отличаетс€ от обычного пути тем, что в н\"ем не может
 быть повтор€ющихс€ вершин.
 
 {\it ѕростой цикл} --- это цикл, в котором нет повтор€ющихс€ вершин и
 р\"ебер.

 ƒан неориентированный граф. ѕосчитайте, сколько в н\"ем различных простых
 циклов. «аметим, что циклы считаютс€ одинаковыми, если они обход€т одно
 и то же множество вершин в одном и том же пор€дке, возможно, начина€ при
 этом из другой вершины, или если пор€док обхода противоположный. Ќапример,
 циклы с пор€дком обхода вершин $1, 2, 3, 1$, $2, 3, 1, 2$ и $1, 3, 2, 1$
 считаютс€ одинаковыми, а циклы $1, 2, 3, 4, 1$ и $1, 3, 4, 2, 1$ --- нет,
 поскольку пор€док обхода вершин различен.

\InputFile

 ¬ первой строке входного файла заданы числа $N$ и $M$ через пробел ---
 количество вершин и р\"ебер в графе, соответственно
 ($1 \leqslant N \leqslant 10$).
 —ледующие $M$ строк содержат по два числа $u_i$ и $v_i$ через пробел
 ($1 \leqslant u_i, \, v_i \leqslant N$, $u_i \neq v_i$);
 кажда€ така€ строка означает, что в графе существует ребро между вершинами
 $u_i$ и $v_i$. ¬ графе нет кратных р\"ебер.

\OutputFile

 ¬ыведите одно число --- количество простых циклов в заданном графе.

\Examples

\begin{example}
\exmp{
3 2
1 2
2 3
}{
0
}%
\exmp{
4 5
1 2
2 3
3 4
4 1
1 3
}{
3
}%
\end{example}

\end{problem}
