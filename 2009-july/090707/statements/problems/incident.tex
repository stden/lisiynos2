\begin{problem}{ћатрица инцидентности}
{incident.in}{incident.out}{2 секунды}{64 мегабайта}

 ¬ершина графа $u$ называетс€ {\it инцидентной} ребру $e$, если $u$
 €вл€етс€ одним из концов ребра $e$.

 јналогично, ребро $e$ называетс€ {\it инцидентным} вершине $u$, если
 один из концов $e$ --- это вершина $u$.

 {\it ћатрицей инцидентности} графа $G = (V, \, E)$ называетс€ пр€моугольна€
 таблица из $|V|$ строк и $|E|$ столбцов, в которой на пересечении $i$-ой
 строки и $j$-го столбца записана единица, если вершина $i$ инцидентна
 ребру $j$, и ноль в противном случае.

 ƒан неориентированный граф. ¬ыведите его матрицу инцидентности.

\InputFile

 ¬ первой строке входного файла заданы числа $N$ и $M$ через пробел ---
 количество вершин и р\"ебер в графе, соответственно
 ($1 \leqslant N \leqslant 100$, $0 \leqslant M \leqslant 10 \, 000$).
 —ледующие $M$ строк содержат по два числа $u_i$ и $v_i$ через пробел
 ($1 \leqslant u_i, \, v_i \leqslant N$);
 кажда€ така€ строка означает, что в графе существует ребро между вершинами
 $u_i$ и $v_i$. –\"ебра нумеруютс€ в том пор€дке, в котором они даны во
 входном файле, начина€ с единицы.

\OutputFile

 ¬ыведите в выходной файл $N$ строк, по $M$ чисел в каждой. $j$-ый элемент
 $i$-ой строки должен быть равен единице, если вершина $i$ инцидентна
 ребру $j$, и нулю в противном случае. –аздел€йте соседние элементы строки
 одним пробелом.

\Examples

\begin{example}
\exmp{
3 2
1 2
2 3
}{
1 0
1 1
0 1
}%
\exmp{
2 2
1 1
1 2
}{
1 1
0 1
}%
\end{example}

\end{problem}
