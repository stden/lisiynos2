.PROBLEM
  .ID = 1                               # идентификатор (номер по счету)
  .Litera = 1                           # литера задачи
  .Name = "Потерянные знаки"            # название
  .Time limit = 10 sec                  # ограничение по времени
  .Tests = 7                            # количество тестов
  .Input file   = input.txt             # вход задачи
  .Output file  = output.txt            # выход задачи
  .Test files   = 1_[1..7].tst          # имена входных тестовых файлов
  .Answer files = 1_[1..7].ans          # имена выходных тестовых файлов
  .TestLib = 1.exe                      # сравнивающий результаты файл
  .Directory = TESTS_1\                 # дира местоположения тестов
  .Status = OK                          # задачу включаем в соревнования

.PROBLEM
  .ID = 2                               # идентификатор (номер по счету)
  .Litera = 2                           # литера задачи
  .Name = "Вавилонская башня"           # название
  .Time limit = 10 sec                  # ограничение по времени
  .Tests = 7                            # количество тестов
  .Input file   = input.txt             # вход задачи
  .Output file  = output.txt            # выход задачи
  .Test files   = 2_[1..7].tst          # имена входных тестовых файлов
  .Answer files = 2_[1..7].ans          # имена выходных тестовых файлов
  .TestLib = 2.exe                      # сравнивающий результаты файл
  .Directory = TESTS_2\                 # дира местоположения тестов
  .Status = OK                          # задачу включаем в соревнования

.PROBLEM
  .ID = 3                               # идентификатор (номер по счету)
  .Litera = 3                           # литера задачи
  .Name = "Большой, Белый, Прямоугольный" # название
  .Time limit = 10 sec                  # ограничение по времени
  .Tests = 10                           # количество тестов
  .Input file   = input.txt             # вход задачи
  .Output file  = output.txt            # выход задачи
  .Test files   = 3_[1..10].tst         # имена входных тестовых файлов
  .Answer files = 3_[1..10].ans         # имена выходных тестовых файлов
  .TestLib = 3.exe                      # сравнивающий результаты файл
  .Directory = TESTS_3\                 # дира местоположения тестов
  .Status = OK                          # задачу включаем в соревнования

.PROBLEM
  .ID = 4                               # идентификатор (номер по счету)
  .Litera = 4                           # литера задачи
  .Name = "Игра"                        # название
  .Time limit = 10 sec                  # ограничение по времени
  .Tests = 9                            # количество тестов
  .Input file   = input.txt             # вход задачи
  .Output file  = output.txt            # выход задачи
  .Test files   = 4_[1..9].tst          # имена входных тестовых файлов
  .Answer files = 4_[1..9].ans          # имена выходных тестовых файлов
  .TestLib = 4.exe                      # сравнивающий результаты файл
  .Directory = TESTS_4\                 # дира местоположения тестов
  .Status = OK                          # задачу включаем в соревнования

.PROBLEM
  .ID = 5                               # идентификатор (номер по счету)
  .Litera = 5                           # литера задачи
  .Name = "Японская полоса"             # название
  .Time limit = 10 sec                  # ограничение по времени
  .Tests = 11                           # количество тестов
  .Input file   = input.txt             # вход задачи
  .Output file  = output.txt            # выход задачи
  .Test files   = 5_[1..11].tst         # имена входных тестовых файлов
  .Answer files = 5_[1..11].ans         # имена выходных тестовых файлов
  .TestLib = 5.exe                      # сравнивающий результаты файл
  .Directory = TESTS_5\                 # дира местоположения тестов
  .Status = OK                          # задачу включаем в соревнования

.PROBLEM
  .ID = 6                               # идентификатор (номер по счету)
  .Litera = 6                           # литера задачи
  .Name = "Ниндзя - Черепашки"          # название
  .Time limit = 10 sec                  # ограничение по времени
  .Tests = 12                           # количество тестов
  .Input file   = input.txt             # вход задачи
  .Output file  = output.txt            # выход задачи
  .Test files   = 6_[1..12].tst         # имена входных тестовых файлов
  .Answer files = 6_[1..12].ans         # имена выходных тестовых файлов
  .TestLib = 6.exe                      # сравнивающий результаты файл
  .Directory = TESTS_6\                 # дира местоположения тестов
  .Status = OK                          # задачу включаем в соревнования

